<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý bình luận</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <h2>Danh sách bình luận</h2>

    <% if (comments.length === 0) { %>
        <p>Không có bình luận nào.</p>
    <% } else { %>
        <table border="1">
            <thead>
                <tr>
                    <th>Người dùng</th>
                    <th>Nội dung</th>
                    <th>Thời gian</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                <% comments.forEach(comment => { %>
                    <tr>
                        <td><%= comment.userId.fullName %></td>
                        <td><%= comment.content %></td>
                        <td><%= new Date(comment.createdAt).toLocaleString() %></td>
                        <td>
                            <form action="/admin/comments/delete/<%= comment._id %>" method="POST" style="display:inline;">
                                <button type="submit" onclick="return confirm('Bạn có chắc chắn muốn xóa mềm bình luận này?')">Xóa mềm</button>
                            </form>
                            <form action="/admin/comments/delete-permanent/<%= comment._id %>" method="POST" style="display:inline;">
                                <button type="submit" onclick="return confirm('Bạn có chắc chắn muốn xóa vĩnh viễn bình luận này?')">Xóa vĩnh viễn</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>

    <a href="/admin/dashboard">Quay lại Dashboard</a>
</body>
</html>
